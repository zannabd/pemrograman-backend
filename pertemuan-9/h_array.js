// Membuat variable array menggunakan const
const animal = ["Badak", "Anjing", "Kuda", "Onta"];

// Mengakses element array menggunakan index
console. log(animal[1], animal[3]);

console.log("====================");
// Looping array menggunakan for of
for (const anml of animal){
    console.log(anml);
}

